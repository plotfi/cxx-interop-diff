@_exported import Foundation

@available(iOS 16.0, *)
@available(swift, obsoleted: 3, renamed: "UIWindowScene.GeometryPreferences")
typealias UIWindowSceneGeometryPreferences = UIWindowScene.GeometryPreferences
extension UIWindowScene {
  @available(iOS 16.0, *)
  @_nonSendable(_assumed) class GeometryPreferences : NSObject {

    /// UIWindowSceneGeometryPreferences is an empty base class and should not be initialized on its own.
    @available(*, unavailable)
    init()
    @available(*, unavailable)
    class func new() -> Self
  }
}
